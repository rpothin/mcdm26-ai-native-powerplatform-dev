Continue the Poutine League employee-facing Power Apps code app build — this is Phase 2 of the agreed phased plan, stacked on top of Phase 1 (PR #27, branch rpothin-redesigned-garbanzo: Submit-a-poutine flow) which is itself stacked on Phase 0 (PR #26: scaffold, all 11 Dataverse data sources, nav shell, design tokens).

Read first: code-apps/employee-app/memory-bank.md (full history + known blocker), code-apps/employee-app/PRODUCT.md, root PRODUCT.md (especially the discovery/browse section — list + map, tags/filtering), root ARCHITECTURE.md (data model: Restaurant, PoutineSubmission, Tag, Try, Review tables), and code-apps/DESIGN.md (reuse code-apps/employee-app/src/styles/tokens.css, don't redefine).

Build Phase 2 — 'Browse/List discovery view':
- A Browse screen replacing the current BrowseScreen.tsx placeholder: a list of approved/published poutine submissions (only submissions in an approved/visible status should show — check the PoutineSubmission status picklist values already used in Phase 1's src/data/submissions.ts for the status enum/shape).
- Each list item: poutine name, restaurant name, photo (if present), tags, and an aggregate indicator if Try/Review data exists (average rating or try count — check the Try/Review tables' shape via the generated MicrosoftDataverseModel/Service in src/generated/).
- Filtering/search: by tag (reuse or extend the TagMultiSelect component from Phase 1 if it fits) and a text search on poutine/restaurant name.
- Tap/click a list item to open a detail view (new component) showing full submission info, restaurant details, tags, and existing tries/reviews for that submission (read-only in this phase — the Try+Review *creation* flow is Phase 3, out of scope here; just display existing data if any).
- No map integration in this phase (Map is Phase 4, separate).
- Use the existing NavShell, EmptyState, StatusBadge (from Phase 1) components and design tokens.
- Use the generated Dataverse service methods / the src/lib/dataverseClient.ts wrapper introduced in Phase 1 for all data access — never hand-edit src/generated/ or use fetch/axios directly.

Gate: run `make app-gate` (lint + test) from the repo root, and `npm run build` inside code-apps/employee-app, before considering this done. Known environment blocker: `pac code push` will still fail with CodeAppOperationNotAllowedInEnvironment (HTTP 403) until an environment admin enables 'Power Apps code apps' — expected, does not block this phase's local build/lint/dev work, do not attempt to work around it.

When done: commit to this branch, run the mandatory Entire traceability protocol (make entire-observe -> entire-link-dry -> entire-link-apply/entire-link-apply-unsafe -> entire-stack-finalize), push, and open a PR based on rpothin-redesigned-garbanzo (this branch, Phase 1) — not main — since this is a stacked layer per the repo's gh-stack convention. Update code-apps/employee-app/memory-bank.md with this phase's completed steps and any autonomous decisions you make (e.g. how you determine 'approved' status, rating aggregation approach).