We are co-defining the implementation plan for the Poutine League **employee-facing Power Apps code app**. Do NOT start building/scaffolding yet — this session is for planning and interviewing me to lock down the plan before implementation begins.

Context to read first (in this order):
1. `code-apps/employee-app/PRODUCT.md` — the employee app's own product context (users ~50 employees, fully responsive mobile+desktop, purpose, positioning, capabilities/constraints, principles, explicitly undecided items like winner rewards and final category list). This was just authored via the impeccable `init` flow on this branch.
2. Root `PRODUCT.md` — whole-product functional vision for Poutine League (submissions, tries/reviews, discovery via list+map, gamification categories/leaderboards, agentic AI features, notifications policy).
3. Root `ARCHITECTURE.md` — technical architecture: Dataverse data model (Restaurant, Poutine Submission, Tag, Try, Review, Season, Category, Season Result, Hall of Fame Entry tables; systemuser-backed identity), Copilot Studio agents (interactive conversational agent embedded in this app + specialized backend agents), Power Automate flows (submission review, geocoding, fun facts, nightly leaderboard computation, season rollover), and the note that this employee app is one of **two** planned code apps (the other being a separate admin/moderation app, out of scope for this session).
4. `code-apps/DESIGN.md` — the shared visual design system ("Comptoir Championship" hand-painted casse-croûte aesthetic, fry-gold/ketchup-red palette, Alfa Slab One + Inter typography, sticker-shadow/pill-shaped component language) that this app must inherit rather than redefine.
5. `docs/dev-solution-conventions.md` and the Dev environment GitHub repo variables (`DEV_ENVIRONMENT_URL`, `DEV_ENVIRONMENT_ID`, `DEV_SOLUTION_ID`) referenced from the root `AGENTS.md` — resolve these for Dev environment/solution context before any Dataverse-touching planning.

Your job:
- Use the `app-implementation` skill (Power Apps Code App orchestrator, code-apps-preview plugin) to guide this planning work — invoke it before doing ad hoc planning, since it enforces the correct patterns/sequencing for code apps in this repo.
- Interview me (the user) to clarify scope, sequencing, and any open decisions needed to produce a solid implementation plan: which screens/flows to build first (submit, browse/list, map, try+review, leaderboards/Hall of Fame, embedded chat agent), how to phase the work into reviewable increments (this repo works in small stacked PRs via `gh-stack`), which Dataverse tables/connections the app needs wired up first, and how the embedded conversational agent integration should be sequenced relative to the rest (e.g. can it be a later phase since the agent itself is built separately via `agent-implementation`).
- Surface and get my explicit decision on anything the PRODUCT.md or ARCHITECTURE.md leaves open that materially affects the plan (e.g. exact leaderboard/category scope for v1, whether map view ships in v1 or a later increment, seed/demo data dependency timing).
- Produce a clear, phased implementation plan (as a plan-mode proposal, not code) that breaks the employee app build into a sequence of small, independently reviewable stacked PR-sized chunks, respecting: `make app-gate` before deploying, working only inside unpacked `solutions/<name>/` folders for any Dataverse-adjacent pieces, and never hand-editing `src/generated/`.

Start by loading the `app-implementation` skill, then confirm your understanding of the above context back to me briefly, then begin the planning interview. Do not scaffold any files or run build tooling yet — this is planning only, we'll move to an implementation session afterward.