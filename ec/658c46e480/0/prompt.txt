Continue the Poutine League employee-facing Power Apps code app build — this is Phase 5 (final phase of the current plan) of the agreed phased plan, stacked on top of Phase 4 (PR #30, branch rpothin-employee-app-map-view: Map view) which stacks on Phase 3 (PR #29: Try+Review) which stacks on Phase 2 (PR #28: Browse/List) which stacks on Phase 1 (PR #27: Submit-a-poutine) which stacks on Phase 0 (PR #26: scaffold). All 5 prior PRs are registered as a native GitHub Stack (Stack #31 via `gh stack`).

Read first: code-apps/employee-app/memory-bank.md (full history, autonomous decisions from Phases 0-4, known blocker), code-apps/employee-app/PRODUCT.md (explicitly flags 'final category list' and 'winner rewards' as undecided — make a reasonable v1 scope decision and document it, don't block on it), root PRODUCT.md (gamification categories/leaderboards section), and root ARCHITECTURE.md — specifically the Season, Category, SeasonResult, SeasonResultEntry, and HallOfFameEntry tables, plus the nightly leaderboard computation Power Automate flow and season rollover flow (these flows compute/populate SeasonResult/SeasonResultEntry/HallOfFameEntry server-side — this code app phase is a READ-ONLY display of that already-computed data, not a computation engine itself). Reuse code-apps/DESIGN.md tokens (code-apps/employee-app/src/styles/tokens.css).

Build Phase 5 — 'Leaderboards / Hall of Fame (v1 scope)':
- Replace the LeaderboardsScreen.tsx placeholder with a real view showing the current/most recent Season's leaderboard: per-Category rankings from SeasonResultEntry (check exact field names in the generated MicrosoftDataverseModel for rpo_seasonresultentry — likely rank, score/count, employee/submission lookup), grouped or tabbed by Category.
- Show a Hall of Fame section: past season winners from HallOfFameEntry (season, category, winning entry).
- If a season/category has no computed results yet (e.g. nightly flow hasn't run), show an empty state (reuse the existing EmptyState component) rather than crashing.
- v1 scope decision needed on exact category list and display depth (e.g. top-3 vs full ranked list per category) — make a reasonable autonomous decision consistent with root PRODUCT.md's gamification intent, and document it clearly in memory-bank.md as an open decision the human product owner should revisit.
- Use the generated Dataverse service methods / src/lib/dataverseClient.ts wrapper for all data access (read-only queries only — no writes needed for this phase, since computation is server-side). Do not hand-edit src/generated/ or use fetch/axios directly.
- Reuse NavShell, EmptyState, StatusBadge, and existing design tokens; keep visual consistency with Browse/Map screens from earlier phases.

Gate: run `make app-gate` (lint + test) from the repo root, and `npm run build` inside code-apps/employee-app, before considering this done. Known environment blocker: `pac code push` will still fail with CodeAppOperationNotAllowedInEnvironment (HTTP 403) until an environment admin enables 'Power Apps code apps' — expected, does not block this phase's local build/lint/dev work, do not attempt to work around it.

When done: commit to this branch, run the mandatory Entire traceability protocol (make entire-observe -> entire-link-dry -> entire-link-apply/entire-link-apply-unsafe -> entire-stack-finalize), push, and open a PR based on rpothin-employee-app-map-view (this branch, Phase 4) — not main — since this is a stacked layer per the repo's gh-stack convention. After opening the PR, also run `gh stack init` (adopting all branches from rpothin-literate-disco through your new branch, base main) and `gh stack submit --auto` from your own worktree to extend the existing GitHub Stack #31 with this new top layer — run this from your own worktree (which already has your new branch checked out, avoiding conflicts with other active worktrees). Update code-apps/employee-app/memory-bank.md with this phase's completed steps, the v1 category/display-depth decision, and note that this is the last phase of the currently-agreed plan (embedded chat agent wiring remains explicitly deferred/out of scope, to be picked up in a future session once the Copilot Studio agent itself exists).