Continue the Poutine League employee-facing Power Apps code app build — this is Phase 4 of the agreed phased plan, stacked on top of Phase 3 (PR #29, branch rpothin-improved-enigma: Try + Review creation flow) which stacks on Phase 2 (PR #28: Browse/List discovery) which stacks on Phase 1 (PR #27: Submit-a-poutine) which stacks on Phase 0 (PR #26: scaffold, Dataverse wiring, nav shell).

Read first: code-apps/employee-app/memory-bank.md (full history, autonomous decisions from Phases 0-3, known blocker), code-apps/employee-app/PRODUCT.md, root PRODUCT.md (the 'discovery via list + map' section), and root ARCHITECTURE.md — specifically the Restaurant table's Latitude/Longitude fields and the geocoding Power Automate flow (this flow populates lat/long asynchronously after a restaurant is created; it may or may not have run for any given restaurant, so map code must handle missing/null coordinates gracefully). Also read code-apps/DESIGN.md for any map-specific styling guidance, and reuse code-apps/employee-app/src/styles/tokens.css.

Build Phase 4 — 'Map view':
- Replace the current MapScreen.tsx placeholder with a real map showing all approved poutine submissions' restaurants as pins/markers (reuse the same 'approved-only visibility' rule established in Phase 2's src/data/submissions.ts).
- Use a lightweight, no-API-key map library appropriate for a React + Vite app (e.g. Leaflet + react-leaflet with OpenStreetMap tiles) — check if one is already a dependency; if not, install the minimal necessary package(s) via npm. Do not introduce a paid/API-key-gated provider (e.g. Google Maps) given this is an internal 50-employee tool with no existing key provisioned.
- Restaurants without geocoded coordinates yet (Latitude/Longitude null) must not crash the map — either skip them or show a small 'ungeocoded' list/notice, your call, document the decision in memory-bank.md.
- Clicking/tapping a pin should show a popup/callout with the restaurant name and its poutine submission(s) at that location, with a way to navigate to the existing SubmissionDetail view (from Phase 2) for a given submission — reuse that component/pattern rather than duplicating it.
- Apply DESIGN.md styling to map controls/popups as much as the map library allows (custom marker icons in fry-gold/ketchup-red if feasible, popup card styled with existing design tokens) — don't fight the library excessively if full re-theming isn't practical, note any limitations in memory-bank.md.
- Use the generated Dataverse service methods / src/lib/dataverseClient.ts wrapper for all data access. Do not hand-edit src/generated/ or use fetch/axios directly.

Gate: run `make app-gate` (lint + test) from the repo root, and `npm run build` inside code-apps/employee-app, before considering this done. Known environment blocker: `pac code push` will still fail with CodeAppOperationNotAllowedInEnvironment (HTTP 403) until an environment admin enables 'Power Apps code apps' — expected, does not block this phase's local build/lint/dev work, do not attempt to work around it.

When done: commit to this branch, run the mandatory Entire traceability protocol (make entire-observe -> entire-link-dry -> entire-link-apply/entire-link-apply-unsafe -> entire-stack-finalize), push, and open a PR based on rpothin-improved-enigma (this branch, Phase 3) — not main — since this is a stacked layer per the repo's gh-stack convention. Update code-apps/employee-app/memory-bank.md with this phase's completed steps, the map library chosen, and autonomous decisions.