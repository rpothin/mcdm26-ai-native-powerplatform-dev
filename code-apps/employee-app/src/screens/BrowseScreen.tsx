import { EmptyState } from "../components/EmptyState/EmptyState";

export function BrowseScreen() {
  return (
    <EmptyState
      eyebrow="Phase 2"
      title="Browse poutines"
      description="Filter approved poutines by tag, neighborhood, or rating, and open a detail view. This discovery feed lands in Phase 2 — for now, this is just the nav shell placeholder."
    />
  );
}
