import { EmptyState } from "../components/EmptyState/EmptyState";

export function SubmitScreen() {
  return (
    <EmptyState
      eyebrow="Phase 1"
      title="Submit a poutine"
      description="Share a new spot: restaurant, poutine name, price, photo, and tags. This form lands in Phase 1 of the build — for now, this is just the nav shell placeholder."
    />
  );
}
