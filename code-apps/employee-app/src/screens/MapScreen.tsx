import { EmptyState } from "../components/EmptyState/EmptyState";

export function MapScreen() {
  return (
    <EmptyState
      eyebrow="Phase 4"
      title="Map view"
      description="Pins for every approved poutine, placed from the restaurant's saved coordinates. This map lands in Phase 4 — for now, this is just the nav shell placeholder."
    />
  );
}
