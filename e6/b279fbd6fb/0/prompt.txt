Continue the Poutine League employee-facing Power Apps code app build — this is Phase 3 of the agreed phased plan, stacked on top of Phase 2 (PR #28, branch rpothin-employee-app-browse-discovery: Browse/List discovery view) which stacks on Phase 1 (PR #27: Submit-a-poutine) which stacks on Phase 0 (PR #26: scaffold, Dataverse wiring, nav shell).

Read first: code-apps/employee-app/memory-bank.md (full history, autonomous decisions from Phases 0-2, known blocker), code-apps/employee-app/PRODUCT.md, root PRODUCT.md (the tries/reviews gamification section), root ARCHITECTURE.md (Try and Review table schemas), and code-apps/DESIGN.md (reuse code-apps/employee-app/src/styles/tokens.css). Also read src/data/tries.ts, src/data/reviews.ts, and src/components/SubmissionDetail/SubmissionDetail.tsx from Phase 2 — that phase already built read-only display of tries/reviews; this phase adds the *creation* flow.

Build Phase 3 — 'Try + Review flow':
- From the SubmissionDetail view (built in Phase 2), let the signed-in employee log a 'Try' of a poutine submission (record that they tried it — check the Try table schema in ARCHITECTURE.md / the generated MicrosoftDataverseModel for the exact fields, e.g. date tried, submission lookup, employee/systemuser lookup).
- After (or as part of) logging a Try, let them optionally leave a Review: rating (check the Review table's rating field — likely a numeric/star scale) plus a text comment.
- Prevent duplicate reviews from the same employee for the same submission if the schema/product intent implies one review per employee per poutine (check ARCHITECTURE.md and root PRODUCT.md for this rule; if ambiguous, make a reasonable autonomous decision and document it in memory-bank.md).
- Update the aggregate display (average rating, try count) introduced in Phase 2 to reflect newly created tries/reviews without requiring a full page reload (refetch or optimistic update after successful create).
- Reuse existing components/patterns: StatusBadge, design tokens, the src/lib/dataverseClient.ts wrapper for all data access. Do not hand-edit src/generated/ or use fetch/axios directly.
- No new screens/routes needed — this should extend the existing SubmissionDetail view from Phase 2 with a 'Log a try' / 'Leave a review' action and form (modal or inline, your call — document the choice).

Gate: run `make app-gate` (lint + test) from the repo root, and `npm run build` inside code-apps/employee-app, before considering this done. Known environment blocker: `pac code push` will still fail with CodeAppOperationNotAllowedInEnvironment (HTTP 403) until an environment admin enables 'Power Apps code apps' — expected, does not block this phase's local build/lint/dev work, do not attempt to work around it.

When done: commit to this branch, run the mandatory Entire traceability protocol (make entire-observe -> entire-link-dry -> entire-link-apply/entire-link-apply-unsafe -> entire-stack-finalize), push, and open a PR based on rpothin-employee-app-browse-discovery (this branch, Phase 2) — not main — since this is a stacked layer per the repo's gh-stack convention. Update code-apps/employee-app/memory-bank.md with this phase's completed steps and autonomous decisions.